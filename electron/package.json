{
  "name": "rust-electron",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "electron-build-env neon build neon_bindings --release",
    "prestart": "npm run build",
    "start": "electron .",
    "predist": "npm run build",
    "dist": "electron-packager .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "neon_bindings": "file:../neon_bindings"
  },
  "devDependencies": {
    "electron": "^7.1.8",
    "electron-build-env": "^0.2.0",
    "electron-packager": "^14.1.1",
    "neon-cli": "^0.3.3"
  }
}
